#!/usr/bin/env ruby

load File.expand_path('../../lib/lib.rb', __FILE__)

if $PROGRAM_NAME == __FILE__
  org = ARGV.shift || "give input org file as first argument"
  html = ARGV.shift || "give output html file as first argument"
  MakeGraph.new(org, html).run
end
